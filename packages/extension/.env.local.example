# Copy this file to .env.local and fill in for local/dev builds.
# .env.local is gitignored.

# Points the extension at your local backend dev server instead of production.
VITE_BACKEND_URL=http://localhost:3000
